# Entry-point behavior checks

An independent subagent explicitly read the skill and necessary references at baseline `25e1db0` on 2026-09-21, without receiving expected answers. It described concrete actions, reference selection, and user decision points for four new requests. It did not implement four complete projects or measure automatic skill selection after installation.

| Request | Observed handling |
| --- | --- |
| Create an explorable air purifier model with no chosen style | Inspect product and host material, form a brief, inspect community references, prepare comparable demos, then wait for a direction; parallelize when tools allow |
| Reduce a plastic appearance while keeping the pale industrial design and layout | Inspect materials, environment, and normals in the actual scene; preserve the direction rather than restarting style selection |
| Fix only keyboard selection and highlight synchronization | Reproduce the keyboard-to-state/update path, make a focused correction, and verify real keys and related pointer behavior without new design research |
| Review only a continuous address space drawn as physical stacked layers | Check spatial encoding against the article; distinguish conditional judgments from observed facts; report evidence and suggestions without edits |

The exercise exposed two ambiguities: requiring extra user confirmation for every completion, and a repair instruction that did not explicitly exclude review-only work. The guidance now reserves waiting for unresolved choices, fixes implementation tasks, and reports suggestions for reviews.

These observations support task routing, not guaranteed future behavior. Complete modeling and sustained aesthetic refinement still require real project use and user evaluation.

## Static scenario review

The author checked the following boundaries against the guidance without a new independent model run or new demos. These are static review conclusions, not additional forward-test passes.

| Request | Expected boundary supported by the text |
| --- | --- |
| Compare two presentations of an existing pump CAD model | Preserve known silhouette, holes, and dimensions; vary composition, lighting, and material presentation rather than reshaping the product |
| Redesign a concept desk lamp, including its housing | Separate variable form from fixed constraints; do not apply existing-product fidelity rules to an authorized new design |
| Adopt the interaction rhythm of a static reference image | Analyze visible form and composition; mark interaction unknown until experienced or demonstrated |
| Compare two brand colors in the same scene | Complete the color comparison without expanding into modeling or page redesign, or claiming structural differences |
| Refine selection feedback after a quiet section view has been chosen | Preserve chosen language and relationships; repair feedback without another theme choice or mandatory design document |
| Review an abstract protocol diagram that needs names | Retain necessary labels and assess whether form/spatial encoding clarifies relationships; do not require protocol recognition from silhouette alone |

Existing-object fidelity and comparable-camera guidance were narrowed to their actual conditions. These checks do not establish an improvement in generated aesthetics. English translation preserves the intended boundaries; it has not received a new independent forward test.
